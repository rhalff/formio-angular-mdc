import { FormioComponents } from 'formiojs/lib/components/Components'
import { mixin, MaterialBaseMixin } from '../../lib/index'

export class MaterialDataGridComponent extends FormioComponents {
}
mixin(MaterialDataGridComponent, [MaterialBaseMixin])
